import {noRawDesignValues} from './no-raw-design-values.mjs';

/** Lint rules that hold boundaries this design system relies on. */
export const rules = {
  'no-raw-design-values': noRawDesignValues,
};

export default {rules};
