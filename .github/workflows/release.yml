name: Release

on:
  push:
    branches: [main]

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  release:
    if: github.repository == 'Misoto22/kioku-ui'
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: write
      id-token: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.10.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - run: pnpm install --frozen-lockfile
      # The audit only launches Chromium; --with-deps adds an apt step that has
      # hung this job indefinitely, and ubuntu-latest already carries the
      # libraries Playwright needs. The timeout keeps a hang from blocking the
      # release queue, which never cancels in-progress runs.
      - run: pnpm exec playwright install chromium
        timeout-minutes: 10
      - run: pnpm release:verify
      - name: Create release PR or publish approved versions
        # Action v2 requires Changesets CLI v3. Keep v1 pinned while this
        # repository intentionally remains on the CLI v2 release contract.
        uses: changesets/action@a45c4d594aa4e2c509dc14a9f2b3b67ba3780d0d # v1.9.0
        with:
          commitMode: github-api
          publish: pnpm release
          github-token: ${{ secrets.GITHUB_TOKEN }}
        env:
          NPM_CONFIG_PROVENANCE: true
